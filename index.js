// Muliply
const num1 = 31;
const num2 = 2;
const multiply = num1 * num2;

// Random
const random = Math.ceil(Math.random() * 20);

// Remainder
const num3 = 24;
const num4 = 20;
const mod = num3 % num4;

// Max
const max = Math.max(2, 4, 7, 8, 20);
